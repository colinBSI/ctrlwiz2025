---
phase: 999.1-stadia-controller-support
plan: 01
subsystem: plugin-host
tags: [wmi, system.management, stadia, vigem, xinput, winforms, appsettings]

# Dependency graph
requires: []
provides:
  - StadiaDetector static class with WMI HID detection (VID_18D1&PID_9400) and one-time ViGEm dialog
  - StadiaViGEmDialogShown UserScopedSetting (bool, default False) in CtrlWizNW.UI
  - System.Management assembly reference in CtrlWizNW.csproj
affects:
  - 999.1-02 (CmdViewpoint call-site wiring — uses StadiaDetector.CheckAndNotify())

# Tech tracking
tech-stack:
  added:
    - System.Management (.NET 4.8 GAC) — WMI Win32_PnPEntity device enumeration
  patterns:
    - internal static utility class in CtrlWiz.NW namespace (no using import needed in CmdViewpoint)
    - UserScopedSetting bool flag via ApplicationSettingsBase for one-time dialog suppression
    - Silent catch {} on non-fatal infrastructure calls (WMI + Process.Start)

key-files:
  created:
    - CtrlWizNW/CtrlWiz.NW/StadiaDetector.cs
  modified:
    - CtrlWizNW.UI/Properties/Settings.settings
    - CtrlWizNW.UI/Properties/Settings.Designer.cs
    - CtrlWizNW/CtrlWizNW.csproj

key-decisions:
  - "Fully-qualified Settings access (CtrlWiz.NW.UI.Properties.Settings.Default) avoids using alias in StadiaDetector.cs"
  - "catch { } (empty, no LogException) keeps StadiaDetector dependency-free — no logger reference needed"
  - "StadiaViGEmDialogShown set true regardless of Yes/No choice — dialog fires once ever, not once per install"

patterns-established:
  - "Pattern: internal static class in CtrlWiz.NW namespace — callable from CmdViewpoint without using directive"
  - "Pattern: WMI Win32_PnPEntity LIKE query for VID/PID substring match — activation-time only, never in poll loop"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-05-05
---

# Phase 999.1 Plan 01: Stadia Controller Detection Foundation Summary

**WMI-based Stadia HID detection (VID_18D1&PID_9400) with one-time ViGEm install dialog and persisted UserScopedSetting flag — foundation for Plan 02 call-site wiring**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-05T05:36:22Z
- **Completed:** 2026-05-05T05:38:05Z
- **Tasks:** 3
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments

- Created `StadiaDetector.cs` (~70 lines) with `IsStadiaPresent()` WMI query and `CheckAndNotify()` one-time dialog
- Added `StadiaViGEmDialogShown` bool UserScopedSetting to both `Settings.settings` (XML) and `Settings.Designer.cs` (generated property) — kept in sync
- Added `<Reference Include="System.Management" />` to `CtrlWizNW.csproj` between System.Drawing and System.Numerics (alphabetical order, no HintPath needed)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create StadiaDetector.cs** - `ca67abd` (feat)
2. **Task 2: Add StadiaViGEmDialogShown setting** - `eb8d9a8` (feat)
3. **Task 3: Add System.Management reference** - `9c3fc08` (feat)

## Files Created/Modified

- `CtrlWizNW/CtrlWiz.NW/StadiaDetector.cs` — new: WMI HID scan + one-time ViGEm install dialog (internal static class, namespace CtrlWiz.NW)
- `CtrlWizNW.UI/Properties/Settings.settings` — added StadiaViGEmDialogShown (System.Boolean, User scope, default False)
- `CtrlWizNW.UI/Properties/Settings.Designer.cs` — added public bool StadiaViGEmDialogShown property with UserScopedSetting attrs
- `CtrlWizNW/CtrlWizNW.csproj` — added System.Management GAC reference

## Decisions Made

- Used fully-qualified `CtrlWiz.NW.UI.Properties.Settings.Default` in StadiaDetector.cs rather than a `using` alias — keeps the class free of import coupling and matches the pattern in CmdViewpoint.cs line 405
- Used empty `catch { }` (not `catch (Exception ex) { ex.LogException(); }`) — StadiaDetector has no logger dependency; WMI failure and browser-launch failure are both non-fatal
- Dialog flag is set to `true` and saved regardless of whether user clicks Yes or No — the intent is that the dialog fires once ever; whether user installs ViGEm is their choice

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None — no hardcoded placeholders or wired-to-empty-data paths. StadiaDetector is a complete, self-contained utility ready for call-site wiring in Plan 02.

## Threat Flags

No new threat surface beyond what was documented in the plan's threat model. All three threats (T-999.1-01 through T-999.1-03) were accepted as specified:
- WMI query wrapped in `catch { }` (DoS accept)
- Process.Start uses hardcoded GitHub URL constant (EoP accept)
- StadiaViGEmDialogShown stores no PII (Info Disclosure accept)

## Next Phase Readiness

- `StadiaDetector.CheckAndNotify()` is ready to be called — Plan 02 adds one line to `CmdViewpoint.ExecuteCommand()` before `Logger.SetUpLogger`
- `System.Management` reference is in the csproj — `ManagementObjectSearcher` will resolve at build time
- Settings binding is wired — `Settings.Default.StadiaViGEmDialogShown` will round-trip correctly on first run

No blockers.

---
*Phase: 999.1-stadia-controller-support*
*Completed: 2026-05-05*
