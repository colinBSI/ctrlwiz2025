---
phase: 999.1-stadia-controller-support
plan: 02
subsystem: plugin-host
tags: [stadia, xinput, vigem, wmi, csharp, navisworks]

# Dependency graph
requires:
  - phase: 999.1-01
    provides: StadiaDetector static class with CheckAndNotify() ready to call
provides:
  - Activation-time Stadia detection wired into CmdViewpoint.ExecuteCommand()
  - StadiaDetector.CheckAndNotify() called before Logger.SetUpLogger — fires before license check
affects:
  - 999.1-03 (build verification — can now compile and test full detection path end-to-end)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Single-line insertion into existing try/catch — no new error handling surface needed

key-files:
  created: []
  modified:
    - CtrlWizNW/CtrlWiz.NW/CmdViewpoint.cs

key-decisions:
  - "No using directive needed — StadiaDetector is in same CtrlWiz.NW namespace as CmdViewpoint"
  - "Call placed before Logger.SetUpLogger so Stadia users see the dialog regardless of license state"
  - "Call is inside existing try/catch(Exception ex) — any exception caught by ex.LogException() + MessageBox"

patterns-established:
  - "Pattern: activation-time utility call inserted before logger setup — no new try/catch wrapper required"

requirements-completed: []

# Metrics
duration: 1min
completed: 2026-05-05
---

# Phase 999.1 Plan 02: Call-Site Wiring Summary

**Single-line insertion connects StadiaDetector.CheckAndNotify() to CmdViewpoint.ExecuteCommand() at plugin activation, firing WMI Stadia detection before the license check**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-05-05T05:40:14Z
- **Completed:** 2026-05-05T05:40:57Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Inserted `StadiaDetector.CheckAndNotify();` at line 405 of `CmdViewpoint.cs`, directly after the `commandId == "default"` early-return guard and before `Logger.SetUpLogger(LoggedApp.NW)`
- Detection fires before the license check — Stadia users see the ViGEm install dialog regardless of license state
- No new using directives, no new error handling — the existing `catch (Exception ex)` block covers any exception from `CheckAndNotify()`

## Task Commits

Each task was committed atomically:

1. **Task 1: Insert StadiaDetector.CheckAndNotify() call in ExecuteCommand** - `c510cc7` (feat)

## Files Created/Modified

- `CtrlWizNW/CtrlWiz.NW/CmdViewpoint.cs` — added one call (`StadiaDetector.CheckAndNotify();`) and one blank line inside `ExecuteCommand()`, between the commandId guard and `Logger.SetUpLogger`

## Decisions Made

- No `using` directive added — `StadiaDetector` is in the `CtrlWiz.NW` namespace (same as `CmdViewpoint`), so it resolves without import
- Placed before `Logger.SetUpLogger` intentionally — ensures the Stadia dialog appears even if licensing fails or skips initialization
- Relied on existing `catch (Exception ex)` wrapper — no additional exception handling needed; any failure from `CheckAndNotify()` is logged via `ex.LogException()` and shown via `MessageBox.Show(ex.Message)`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None — the call is fully wired. `StadiaDetector.CheckAndNotify()` (built in Plan 01) is complete and callable.

## Threat Flags

No new threat surface. The two documented threats (T-999.1-04: DoS from UI-thread WMI call, T-999.1-05: Tampering via ExecuteCommand modification) were both accepted as specified — `CheckAndNotify()` returns fast and is inside the existing error handler.

## Next Phase Readiness

- Full detection path is wired end-to-end: `CmdViewpoint.ExecuteCommand()` → `StadiaDetector.CheckAndNotify()` → WMI query → one-time ViGEm dialog
- Plan 03 (build verification) can now compile with `Debug NW25|x64` and confirm no compilation errors from the `System.Management` reference and the new call site
- No blockers

---
*Phase: 999.1-stadia-controller-support*
*Completed: 2026-05-05*
