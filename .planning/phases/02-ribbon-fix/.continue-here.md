---
context: phase
phase: 02-ribbon-fix
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-05-04T12:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: DLL-NOT-LOADING — CtrlWizNW.dll is NOT being loaded by NW2025 at all. The XAML fix is complete but the ribbon still doesn't appear because NW2025 cannot resolve the DLL's dependencies. Do NOT spend time re-investigating XAML — that layer is solved.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| XAML-is-root-cause | Assuming XAML parse failure was THE only root cause. XAML is fixed and BAML is correct — but there is a deeper DLL dependency resolution failure in NW2025. | blocking | Run the static ctor test (check for CtrlWizNW_load.txt in %TEMP%) before debugging any ribbon/XAML layer. File absent = DLL not loading. |
| Surface-verification | Trusting that Plugin Manager "Enabled" means DLL loaded. NW2025 shows Enabled based on bundle manifest, not actual DLL load success. | advisory | Always verify with static ctor file or equivalent before declaring DLL loading works. |

<current_state>
Phase 2, Plan 04 (02-04: NWRibbonButton → RibbonButton XAML fix), Task 3 of 3.

Tasks 1 and 2 are COMPLETE:
- CustomRibbon.xaml updated (no NWRibbonButton, no xmlns:local, 5 RibbonButton elements) — commit 2265b65
- Debug NW25|x64 rebuilt and deployed — CtrlWizNW.dll and CustomRibbon.xaml both correct in Contents/Nw25/

Task 3 (human verify) is BLOCKED by DLL load failure discovered during checkpoint:
- Static constructor diagnostic was added to CmdViewpoint.cs — writes CtrlWizNW_load.txt to %TEMP% on DLL load
- After NW2025 relaunch, the file was NOT created → DLL is NOT loading at all
- Root cause: NW2025 cannot resolve CtrlWizNW.dll's dependencies (CtrlWizNW.UI.dll, XInputDotNetPure.dll) from the bundle directory

NOTE: The diagnostic static constructor is still in CmdViewpoint.cs — remove it before shipping.
</current_state>

<completed_work>

- Task 1: Replace NWRibbonButton with RibbonButton in CustomRibbon.xaml — DONE (commit 2265b65)
  - Removed xmlns:local declaration
  - Replaced all 5 local:NWRibbonButton with RibbonButton (AdWindows default namespace)
  - Verified: XAML parses correctly via WPF XamlReader test
  - Verified: BAML in DLL also updated (no NWRibbonButton)
- Task 2: Rebuild Debug NW25|x64 — DONE
  - MSBuild via solution file succeeded
  - Deployed CustomRibbon.xaml and CtrlWizNW.dll confirmed correct in Contents/Nw25/
</completed_work>

<remaining_work>

- Investigate and fix NW2025 dependency resolution failure:
  - CtrlWizNW.dll depends on CtrlWizNW.UI.dll and XInputDotNetPure.dll (and others) in the bundle
  - NW2025 CLR cannot find these dependencies (confirmed via ReflectionTypeLoadException in test)
  - Hypothesis: NW2025's plugin loader uses a different AppDomain or AssemblyLoadContext that doesn't probe the bundle directory
  - Fix options:
    a) Check C:\Program Files\Autodesk\Navisworks Manage 2025\roamer.exe.config for probing paths
    b) Test: copy CtrlWizNW.UI.dll + XInputDotNetPure.dll to C:\Program Files\Autodesk\Navisworks Manage 2025\
    c) Add an AssemblyResolve handler to a bootstrap entry point DLL
    d) Use a different Plugin registration approach that NW2025 supports
- Once DLL loads: Task 3 human verify (check ribbon tab, all 5 buttons, no errors)
- Remove diagnostic static constructor from CmdViewpoint.cs before shipping
</remaining_work>

<decisions_made>

- Replaced local:NWRibbonButton with Autodesk.Windows.RibbonButton — confirmed RibbonButton exists in NW2025 AdWindows with correct properties (Id, Size, ShowText, Orientation)
- Added static ctor diagnostic to confirm DLL load status — definitively proved DLL is not loading
- XAML fix approach is correct; the new blocker is a separate dependency resolution layer
</decisions_made>

<blockers>

- NW2025 dependency resolution: CtrlWizNW.dll's project-reference dependencies (CtrlWizNW.UI.dll, XInputDotNetPure.dll, etc.) cannot be found by NW2025's CLR when loading from the bundle directory. NW2022 had an AssemblyResolve handler that probed the plugin directory — NW2025 either changed this or uses a different loading context.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-ribbon-fix/02-04-PLAN.md` — plan being executed; Task 3 is the human verify checkpoint
2. `.planning/phases/02-ribbon-fix/02-VERIFICATION.md` — prior verification results (XAML layer gaps now fixed)
3. `CtrlWizNW/CtrlWiz.NW/CmdViewpoint.cs` lines 46-60 — diagnostic static ctor (remove before ship)

## Infrastructure State
- `C:\ProgramData\Autodesk\ApplicationPlugins\CtrlWiz.NW.bundle\Contents\Nw25\CustomRibbon.xaml` — correct (RibbonButton, no NWRibbonButton)
- `C:\ProgramData\...Contents\Nw25\CtrlWizNW.dll` — rebuilt 2026-05-04 08:36 with diagnostic static ctor
- All dependency DLLs present in Contents/Nw25/ (timestamps 2026-05-04 08:10) but NW2025 cannot find them

<context>
The XAML fix (plan 02-04 Tasks 1-2) is fully complete and correct. The XAML layer was the stated root cause from the verifier, and it IS fixed. However, fixing the XAML exposed a deeper failure: NW2025 can't load CtrlWizNW.dll at all because of a CLR dependency resolution issue. This is a new finding that was not anticipated in the plan.

The dependency issue is likely why the plugin NEVER worked in NW2025, even before the XAML was broken — the XAML failure masked this deeper issue because both manifest as "ribbon tab not appearing."

Investigation should start by checking roamer.exe.config (or Navisworks.exe.config) in the NW2025 install for privatePath/probing configuration. The simplest test is to copy the dependency DLLs to the NW2025 install directory and see if CtrlWizNW_load.txt appears after relaunch.
</context>

<next_action>
Start with: Check `C:\Program Files\Autodesk\Navisworks Manage 2025\roamer.exe` or equivalent .exe.config for assembly probing paths. Then test hypothesis by copying CtrlWizNW.UI.dll and XInputDotNetPure.dll to the NW2025 install dir and relaunching. If CtrlWizNW_load.txt appears in %TEMP%, dependency probing is the issue and we need a long-term fix (not just copying DLLs there).
</next_action>
