---
phase: 02-ribbon-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/ProgramData/Autodesk/ApplicationPlugins/CtrlWiz.NW.bundle/PackageContents.xml
autonomous: true
requirements:
  - RIBBON-01
  - RIBBON-02

must_haves:
  truths:
    - "The Nw25 ComponentEntry in PackageContents.xml has a unique AppName distinct from the Nw22 entry"
    - "The Nw22 ComponentEntry AppName is unchanged (AppName=CtrlWizNW)"
    - "The Nw25 RuntimeRequirements SeriesMin/Max and ModuleName are unchanged"
  artifacts:
    - path: "C:/ProgramData/Autodesk/ApplicationPlugins/CtrlWiz.NW.bundle/PackageContents.xml"
      provides: "Unique AppName on the Nw25 component entry"
      contains: "AppName=\"CtrlWizNW25\""
  key_links:
    - from: "PackageContents.xml Nw25 ComponentEntry"
      to: "Navisworks 2025 plugin loader"
      via: "AppName uniqueness — loader may silently skip duplicate AppNames"
      pattern: "AppName=\"CtrlWizNW25\""
---

<objective>
Rename the Nw25 ComponentEntry AppName in PackageContents.xml from "CtrlWizNW" to "CtrlWizNW25" to eliminate the duplicate AppName collision between the Nw22 and Nw25 component entries.

Purpose: DIAGNOSIS.md identified this as a RECOMMENDED fix — the AppName collision could not be confirmed or ruled out as a contributing factor because CLR was never reached (no Fusion log evidence). Eliminating the duplicate costs nothing and removes uncertainty before the first real NW2025 plugin load attempt after the csproj fix.
Output: Updated PackageContents.xml with a unique AppName on the Nw25 entry.
</objective>

<execution_context>
@C:\Users\cemch-wei\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\cemch-wei\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-diagnose/01-DIAGNOSIS.md

<interfaces>
<!-- Current state of PackageContents.xml. Executor must not re-read unless confirming line numbers. -->

Current content of C:\ProgramData\Autodesk\ApplicationPlugins\CtrlWiz.NW.bundle\PackageContents.xml:

```xml
<?xml version="1.0" encoding="utf-8"?>
<ApplicationPackage SchemaVersion="1.0" AutodeskProduct="Navisworks" ProductType="Application" AppVersion="2.0.1" Name="CtrlWizNW"
Description="Control and Navigate your BIM model with an Xbox Controller."  SupportedLocales="Enu"
OnlineDocumentation="https://help.buildfore.com/ctrlwiz/ctrlwiz-for-navisworks/how-to-use-ctrlwiz-navisworks"
ProductCode="AE75327E-AB03-4C93-BE10-19DA596C6969" Icon="./Contents/Xbox.ico">
  <CompanyDetails Name="VIATechnik, LLC" Url="https://www.viatechnik.com/" Email="support@viatechnik.com" />
  <Components Description="Navisworks 2022">
    <RuntimeRequirements OS="Win64" Platform="NAVMAN|NAVSIM" SeriesMin="Nw22" SeriesMax="Nw22" />
    <ComponentEntry AppName="CtrlWizNW" AppType="ManagedPlugin" Version="2.0.1" ModuleName="./Contents/Nw22/CtrlWizNW.dll" AppDescription="CtrlWiz - Xbox Controller for Navis" />
  </Components>
  <Components Description="Navisworks 2025">
    <RuntimeRequirements OS="Win64" Platform="NAVMAN|NAVSIM" SeriesMin="Nw25" SeriesMax="Nw25" />
    <ComponentEntry AppName="CtrlWizNW" AppType="ManagedPlugin" Version="2.0.1" ModuleName="./Contents/Nw25/CtrlWizNW.dll" AppDescription="CtrlWiz - Xbox Controller for Navis" />
  </Components>
</ApplicationPackage>
```

Target change: line 13 — `AppName="CtrlWizNW"` on the Nw25 ComponentEntry becomes `AppName="CtrlWizNW25"`.
All other attributes on that line remain unchanged.
</interfaces>

IMPORTANT: This file is in C:\ProgramData\, which requires Admin By Request elevation to write. The executor must acquire elevation before editing this file.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Nw25 AppName to CtrlWizNW25 in PackageContents.xml</name>
  <files>C:/ProgramData/Autodesk/ApplicationPlugins/CtrlWiz.NW.bundle/PackageContents.xml</files>
  <read_first>
    - C:\ProgramData\Autodesk\ApplicationPlugins\CtrlWiz.NW.bundle\PackageContents.xml — read the file to confirm current state before editing (elevation required for the write; reading does not require elevation)
  </read_first>
  <action>
Edit the Nw25 ComponentEntry in PackageContents.xml. Change the single attribute value on the Nw25 ComponentEntry line:

FROM:
```xml
    <ComponentEntry AppName="CtrlWizNW" AppType="ManagedPlugin" Version="2.0.1" ModuleName="./Contents/Nw25/CtrlWizNW.dll" AppDescription="CtrlWiz - Xbox Controller for Navis" />
```

TO:
```xml
    <ComponentEntry AppName="CtrlWizNW25" AppType="ManagedPlugin" Version="2.0.1" ModuleName="./Contents/Nw25/CtrlWizNW.dll" AppDescription="CtrlWiz - Xbox Controller for Navis" />
```

The only change is `AppName="CtrlWizNW"` -> `AppName="CtrlWizNW25"` on the Nw25 entry. Everything else is identical to the current file.

Do NOT change:
- The Nw22 ComponentEntry (it retains `AppName="CtrlWizNW"`)
- The ApplicationPackage `Name="CtrlWizNW"` attribute at the top
- SeriesMin/Max, ModuleName, AppType, Version, AppDescription on the Nw25 entry
- Any other attribute or element in the file

Elevation note: C:\ProgramData\ requires Admin By Request elevation. Use elevation before writing.
  </action>
  <verify>
    Read C:\ProgramData\Autodesk\ApplicationPlugins\CtrlWiz.NW.bundle\PackageContents.xml and confirm:

    1. The Nw25 ComponentEntry line contains `AppName="CtrlWizNW25"` (new value)
    2. The Nw22 ComponentEntry line still contains `AppName="CtrlWizNW"` (unchanged)
    3. The file contains exactly one occurrence of `AppName="CtrlWizNW25"` and at least one occurrence of `AppName="CtrlWizNW"` (the Nw22 entry)
    4. `ModuleName="./Contents/Nw25/CtrlWizNW.dll"` is still present on the Nw25 entry
    5. `SeriesMin="Nw25" SeriesMax="Nw25"` is still present in the Nw25 RuntimeRequirements
  </verify>
  <acceptance_criteria>
    - PackageContents.xml contains `AppName="CtrlWizNW25"` exactly once (on the Nw25 ComponentEntry)
    - PackageContents.xml contains `AppName="CtrlWizNW"` at least once (on the Nw22 ComponentEntry — unchanged)
    - PackageContents.xml contains `ModuleName="./Contents/Nw25/CtrlWizNW.dll"` (Nw25 module path unchanged)
    - PackageContents.xml is valid XML
  </acceptance_criteria>
  <done>PackageContents.xml Nw25 ComponentEntry has AppName="CtrlWizNW25". The Nw22 entry is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation -> C:\ProgramData\ | Requires Admin By Request elevation; restricts unauthorized writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | PackageContents.xml | accept | Protected by filesystem ACL requiring elevation. Developer machine only; no external access path. |
| T-02-04 | Spoofing | AppName="CtrlWizNW25" | accept | AppName is a manifest identifier used by NW's plugin loader, not an authentication token. No security implication from the rename. |
</threat_model>

<verification>
After the task completes:

```powershell
Select-String -Path 'C:\ProgramData\Autodesk\ApplicationPlugins\CtrlWiz.NW.bundle\PackageContents.xml' -Pattern 'AppName'
```

Expected output contains:
- `AppName="CtrlWizNW"` on the Nw22 ComponentEntry line
- `AppName="CtrlWizNW25"` on the Nw25 ComponentEntry line

No line should contain `AppName="CtrlWizNW"` for the Nw25 entry (i.e., no line with both `Nw25/CtrlWizNW.dll` and `AppName="CtrlWizNW"`).
</verification>

<success_criteria>
- PackageContents.xml has unique AppNames: Nw22 entry uses CtrlWizNW, Nw25 entry uses CtrlWizNW25
- No other attributes modified
- File is valid XML
</success_criteria>

<output>
After completion, create `.planning/phases/02-ribbon-fix/02-02-SUMMARY.md`
</output>
